{{#if listExists}}
<ul style="margin: 0px; padding: 0px" id="prodList">
    {{#each productos}}
    {{!-- <tr>
        <td>
            <div class="p-2">
                <img src="{{this.foto}}" alt="{{this.nombre}}" class="img-fluid rounded shadow-sm" style="width: 15rem;">
                <div class="ml-3 d-inline-block align-middle">
                    <h5 class="mb-0 text-dark d-inline-block">{{this.nombre}}</h5>
                <p class="descripcion">{{this.descripcion}}</p>
                </div>
            </div>
        </td>
        <td class="align-middle">{{this.precio}}</td>
        <td class="align-middle">{{this.stock}}</td>
        <td class="align-middle">
            <a href="" onclick=""><i class="large material-icons">add_circle_outline</i></a>
        </td>
    </tr> --}}
    {{/each}}
</ul>
{{else}}
<ul style="margin: 0px; padding: 0px" id="prodList">
    <div class="row align-items-center" style="background-color:#aaa">
        <h1>No hay productos</h1>
    </div>
</ul>
{{/if}}