    <div class="container mt-5 mb-5">
        <div class="row">
            <div class="col-2"></div>
            <div class="col-8">

                    <div>
                        {{> usuario}}
                    </div>
                {{#if userExists}}
                <div class="box_form card-footer" >
                    {{> form}}
                    <br>
                    <br>
                </div>
                {{/if}}
                <div class="box_form card-footer" >
                    {{> lista}}
                    {{!-- {{> chat }} --}}
                </div>
            </div>
            <div class="col-2"></div>
        </div>
    </div>
